<?php

declare(strict_types=1);

// Listed under autoload.files: loaded eagerly, so it must NOT be reported as
// no_declarations even though it declares no types.
define('DOCTOR_PROJECT_BOOTSTRAPPED', true);
